
services:
 db:
   image: postgres:18-alpine
   container_name: vigility-postgres
   restart: always
   environment:
     POSTGRES_USER: vigility
     POSTGRES_PASSWORD: vigility123
     POSTGRES_DB: vigility_db
   ports:
     - "5432:5432"
   volumes:
     - postgres_data:/var/lib/postgresql/data


volumes:
 postgres_data: